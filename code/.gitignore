# secrets / env
.env
.env.*
!.env.example

# runtime state
state/
sandbox/
usage.json

# LabLens run artifacts (screenshots + replay JSON) — large, generated
run_artifacts/**/*.png
run_artifacts/**/*.json
!run_artifacts/.gitkeep
# bc_f847b9ef is the pinned demo run — intentionally committed
!run_artifacts/bc_f847b9ef/**/*.png
!run_artifacts/bc_f847b9ef/**/*.json

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# os
.DS_Store
